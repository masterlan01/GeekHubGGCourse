int last = 65
int first = 2
assert last**first == 4225
assert last**first == Math.pow(65,2)
assert last**first == Math.round(Math.exp(first * Math.log(last)))
assert 65**2 == Math.round(Math.exp(2 * Math.log(65)))
assert last**first == 4224